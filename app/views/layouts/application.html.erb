<!DOCTYPE html>
<html>
<head>
  <title>Salutr</title>
  <script src="//js.pusher.com/2.2/pusher.min.js"></script>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  
  <%if signed_in?%>
  <script>
    window.CHANNEL = "<%= current_user.id%>";
    window.PUSHER = "<%= ENV.fetch('PUSHER_APP_KEY')%>";
  </script>
  <%end%>

  </head>
<body>
<h1> <%= link_to "Salut!", root_path %> </h1>
<% if signed_in?%>
  <div>You are currently signed in as <%= current_user.username%></div>
  <%= link_to "Sign out", sign_out_path %></h1>
  <% else%>
  <%= link_to "Sign up", sign_up_path %>
  <%end%>
<%= yield %>

</body>
</html>
